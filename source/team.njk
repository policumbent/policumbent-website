---
pagination:
  data: site.languages
  size: 1
  alias: lang
  addAllPagesToCollections: true
layout: base.njk
permalink: "/{{ lang.locale }}/team/"
noLangUrl: /team/
sitemap:
  priority: 0.9
  changefreq: daily
eleventyComputed:
  metadata:
    title: "Team"
    description: "{{ 'team_description' | i18n | upfirst | safe }}"
  locale: "{{lang.locale}}"
---

<h1 class="upfirst">{{ 'about_us' | i18n }}</h1>
<div class="spacer-1"></div>
<p class="col-7">
  Siamo un team studentesco del Politecnico Di Torino, lorem ipsum dolor sit amet, consectetur adipiscing elit. Integer vel placerat massa. Morbi ac luctus libero. Donec et arcu quis eros euismod sodales. Vivamus pellentesque, massa in rhoncus molestie, orci turpis elementum ligula, at iaculis purus metus ut libero. Donec pharetra quam augue, nec imperdiet lectus egestas tincidunt.
</p>
<div class="spacer-1"></div>
<img class="col-12 team-photo" src="/static/img/team-wide.jpg" alt="policumbent team in nevada"/>
<div class="spacer-4"></div>

<section class="people-container">
  {% for department in team %}
    {% if department.members.length > 0 %}
      <div class="department-container">
        <h1>{% if locale=="it" %}{{ department.name_it }}{% else %}{{ department.name_en }}{% endif %}</h1>
        <div class="cards-container indented-2">
          {% for member in department.members %}
            <div class="person-card">
              <img src="{{ member.photo.url }}" alt="{{ member.name }} {{ member.surname }} portrait"/>
              <p>{{ member.name }}&nbsp;{{ member.surname }}</p>
              {% if member.urlText and member.url %}
                <a href="{{ member.url }}" target="_blank">{{ member.urlText }}</a>
              {% endif %}
            </div>
          {% endfor %}
        </div>
      </div>
    {% endif %}
  {% endfor %}
</section>